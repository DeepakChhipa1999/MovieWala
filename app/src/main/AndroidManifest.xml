<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tmbd.cinematics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/clapperboard1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/clapperboard1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScrollingActivity2" />
        <activity android:name=".TvShowsActivity" />
        <activity android:name=".ActorDetailActivity" />
        <activity android:name=".DiscoverActivity" />
        <activity android:name=".PopularPeopleActivity" />
        <activity android:name=".SettingActivity" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/title_activity_collapsing_toolbar"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CollapsingToolbar"
            android:label="@string/title_activity_scrolling2"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>